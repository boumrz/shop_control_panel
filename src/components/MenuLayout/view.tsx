import React from 'react';
import { MainContainer } from './styles';

export const MenuLayoutView = (props: any) => {
    return (
        <MainContainer>
            Menu Layout
        </MainContainer>
    );
}